# Standalone LabVIEW VM credentials — consult, log in, activate each installed LabVIEW version.
# Each row is one independent standalone VM. guest_password is the local console/login password.
# NOTE: `actor`/`actor` is the DEV-DEFAULT (from build-virtualbox.sh GUEST_PASSWORD) for local dev VMs on a
# PUBLIC repo; it is NOT a secret. For distributed VMs, set a per-VM GUEST_PASSWORD at build time and keep
# real credentials in an operator-held copy of this CSV (do not commit real passwords to a public repo).
vm_name,hypervisor,guest_user,guest_password,labview_versions,lbabus,notes
lba-ubuntu2404-labview2026-scratch,virtualbox,actor,actor,2026,build-from-source-on-first-boot,"Built from a stock Ubuntu 24.04.4 ISO; LabVIEW 2026 Community ACTIVATED by operator; lbabus self-builds on first boot (no baked binary)."

# Legacy scratch dir from earlier runs -- the export now writes the COMMITTED exported-corpus/ instead
# (the live GPU drive, leg 3, needs the manifest + metrics on a host without these Windows VMs).
fetched/
